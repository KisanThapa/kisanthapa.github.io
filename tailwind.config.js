module.exports = {
    content: ['./*.html', './*.js'], // Specify the HTML files to scan for classes
    theme: {
        extend: {
            fontFamily: {
                roboto: ['Roboto', 'sans-serif'],
            },
        },
    },
    plugins: [],
};
